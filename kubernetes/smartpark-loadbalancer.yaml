apiVersion: v1
kind: Service
metadata:
  name: smartpark-public
spec:
  # Apply after baseline testing; GKE provisions a public external IP.
  type: LoadBalancer
  selector:
    app: smartpark-api
  ports:
    - name: http
      port: 80
      targetPort: http
